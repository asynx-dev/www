#!/usr/bin/env bash

# this file is used to build the site on Linux platform with localhost
# configuration. Not suitable for development phase since this will install
# and destroy the virtual environment.
# Please check README.md for proper flow for development

pipenv install
pipenv run mkdocs build --clean --verbose --config-file mkdocs.yml
pipenv --rm
