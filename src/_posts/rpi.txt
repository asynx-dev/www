bash

sudo fdisk -l
mount | grep /dev/sdb

sudo umount /dev/sdb+([0-9])

sudo dd if=/dev/sdb of=rpi.img bs=1M status=progress

bütün diski kopyaladı ref: https://serverfault.com/questions/439128/dd-on-entire-disk-but-do-not-want-empty-portion

sudo fdisk -l
mount | grep /dev/sdb

sudo umount /dev/sdb+([0-9])

sudo dd of=/dev/sdb if=rpi.img bs=1M status=progress
sync

# Bundan sonrası Rpi üzerinde

df -h

sudo raspi-config
6 Advanced Options -> A1 Expand Filesystem

reboot

df -h


